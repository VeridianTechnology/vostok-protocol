[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback — client-routed pages such as /radio must serve index.html
# on direct loads and refreshes. Static assets still match first.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
